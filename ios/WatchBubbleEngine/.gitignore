# SwiftPM build output. Regenerated by `swift build` / `swift test`, and 200MB+
# of object files and .swiftmodules that change on every compile — it has no
# business in the history.
.build/
.swiftpm/
