# Green Wand Lock Notes

The Green Wand sandbox code is the bible.

Preserve:

```txt
healthy base bubble
→ raw probe dots
→ accepted continuity dots
→ ridge mini-lines
→ magnetic pull/deformation
→ outer shell
→ inset/playable contour
```

The GPS wrapper should provide only:

- locked map frame
- pinned green centre
- metres-per-pixel scale
- clean satellite sample
- result import/export

Do not tune Wand logic if the sample source is invalid.

If Sample Check is black, empty, mixed, or not centred on the pinned green, fix the sample source first.
