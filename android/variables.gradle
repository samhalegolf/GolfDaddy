ext {
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
    /* Connect IQ Mobile SDK for Android, from Maven Central. Note this is
       NOT the version the garmin/connectiq-android-sdk README prints (2.2.0);
       that README is stale. 2.4.0 is the current release — checked against
       repo1.maven.org's maven-metadata.xml on 2026-09-20. */
    connectIqSdkVersion = '2.4.0'
}